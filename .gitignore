*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Working design notes. Kept on disk, never committed.
/docs/

# Paths that belong to one machine, not to the project.
/tools/local.env

# The IDE's own state, all of it.
/.idea/
# Core dumps, which a crash in the C++ harnesses drops in the tree root.
/core.*
